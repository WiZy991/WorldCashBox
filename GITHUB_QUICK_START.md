# 🚀 Быстрая загрузка на GitHub

## Пошаговая инструкция:

### 1. Проверьте, что Git установлен
Откройте терминал и выполните:
```bash
git --version
```

### 2. Инициализируйте Git (если еще не сделано)
```bash
git init
```

### 3. Настройте Git (если еще не настроено)
```bash
git config user.name "Ваше Имя"
git config user.email "ваш@email.com"
```

### 4. Добавьте все файлы
```bash
git add .
```

### 5. Создайте первый коммит
```bash
git commit -m "Initial commit: WorldCashBox website"
```

### 6. Создайте репозиторий на GitHub

1. Зайдите на [github.com](https://github.com) и войдите
2. Нажмите на **"+"** → **"New repository"**
3. Название: `worldcashbox-website`
4. Описание: "Сайт WorldCashBox"
5. Выберите **Public** или **Private**
6. **НЕ добавляйте** README, .gitignore, license
7. Нажмите **"Create repository"**

### 7. Подключите и отправьте код

Замените `YOUR_USERNAME` на ваш GitHub username:

```bash
git remote add origin https://github.com/YOUR_USERNAME/worldcashbox-website.git
git branch -M main
git push -u origin main
```

**Готово!** 🎉

---

## Если возникла ошибка аутентификации:

GitHub больше не принимает пароли. Используйте Personal Access Token:

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token → дайте название → выберите срок действия
3. Отметьте `repo` (полный доступ к репозиториям)
4. Нажмите "Generate token"
5. Скопируйте токен и используйте его вместо пароля при `git push`

---

## Обновление проекта в будущем:

```bash
git add .
git commit -m "Описание изменений"
git push
```

