# 🚀 Инструкция по загрузке проекта на GitHub

## Шаг 1: Проверка .gitignore

Убедитесь, что файл `.gitignore` содержит следующие правила:

```
# Dependencies
node_modules/
/.pnp
.pnp.js

# Testing
/coverage

# Next.js
/.next/
/out/

# Production
/build

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files
.env*.local
.env

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Временные файлы
requests/
*.log

# Большие файлы и медиа (опционально)
# Photo/
# *.zip
```

## Шаг 2: Инициализация Git репозитория

Если Git еще не инициализирован, выполните:

```bash
git init
```

## Шаг 3: Настройка Git (если еще не настроено)

```bash
git config user.name "Ваше Имя"
git config user.email "ваш.email@example.com"
```

## Шаг 4: Добавление файлов

```bash
# Добавить все файлы
git add .

# Проверить, что будет закоммичено
git status
```

## Шаг 5: Первый коммит

```bash
git commit -m "Initial commit: WorldCashBox website"
```

## Шаг 6: Создание репозитория на GitHub

1. Зайдите на [GitHub.com](https://github.com)
2. Нажмите на кнопку **"+"** в правом верхнем углу
3. Выберите **"New repository"**
4. Заполните:
   - **Repository name**: `worldcashbox-website` (или другое название)
   - **Description**: "Сайт WorldCashBox - каталог оборудования и услуг"
   - **Visibility**: Public или Private (на ваше усмотрение)
   - **НЕ добавляйте** README, .gitignore или license (они уже есть)
5. Нажмите **"Create repository"**

## Шаг 7: Подключение к GitHub и отправка

GitHub покажет инструкции. Выполните:

```bash
# Добавить удаленный репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/worldcashbox-website.git

# Или если используете SSH:
# git remote add origin git@github.com:YOUR_USERNAME/worldcashbox-website.git

# Переименовать ветку в main (если нужно)
git branch -M main

# Отправить код на GitHub
git push -u origin main
```

## Важные замечания

### ⚠️ Что НЕ должно попасть в репозиторий:

1. **Секретные данные:**
   - `.env` файлы с API ключами
   - Токены доступа
   - Пароли

2. **Большие файлы:**
   - Папка `Photo/` (если она большая)
   - ZIP архивы
   - node_modules/

3. **Временные файлы:**
   - Папка `requests/` с JSON файлами
   - Логи

### ✅ Что должно быть в репозитории:

- Все исходники кода
- `package.json` и `package-lock.json`
- `next.config.js`
- `tsconfig.json`
- Конфигурационные файлы
- Папка `public/images/products/` (с изображениями)
- `catalog.json`

## Шаг 8: Проверка

После отправки проверьте на GitHub:
- Все ли файлы загрузились
- Открывается ли README.md
- Правильно ли отображается структура проекта

## Если возникли проблемы

### Ошибка аутентификации:
```bash
# Используйте Personal Access Token вместо пароля
# Создайте токен: GitHub Settings > Developer settings > Personal access tokens
```

### Отменить последний коммит:
```bash
git reset --soft HEAD~1
```

### Удалить файлы из отслеживания:
```bash
git rm --cached filename
```

### Игнорировать уже отслеживаемые файлы:
```bash
git rm -r --cached .
git add .
git commit -m "Update .gitignore"
```

## Полезные команды

```bash
# Проверить статус
git status

# Просмотреть историю
git log

# Изменить последний коммит
git commit --amend -m "Новое сообщение"

# Добавить изменения к последнему коммиту
git add .
git commit --amend --no-edit
```

## Обновление проекта на GitHub

После внесения изменений:

```bash
git add .
git commit -m "Описание изменений"
git push
```

