#!/bin/bash

# Скрипт для подготовки проекта к деплою на Beget

echo "🚀 Подготовка проекта к деплою на Beget..."

# Проверка наличия node_modules
if [ ! -d "node_modules" ]; then
    echo "📦 Установка зависимостей..."
    npm install
fi

# Сборка проекта
echo "🔨 Сборка проекта..."
npm run build

# Проверка успешности сборки
if [ $? -eq 0 ]; then
    echo "✅ Сборка завершена успешно!"
    echo ""
    echo "📁 Файлы для загрузки на Beget:"
    echo "   - .next/standalone/ (вся папка)"
    echo "   - .next/static/ (вся папка)"
    echo "   - public/ (вся папка)"
    echo ""
    echo "📝 Следующие шаги:"
    echo "   1. Подключитесь к Beget через FTP/SFTP"
    echo "   2. Загрузите указанные папки в корень вашего сайта"
    echo "   3. На сервере выполните: npm install --production"
    echo "   4. Настройте Node.js в панели управления Beget"
    echo ""
    echo "📖 Подробная инструкция в файле DEPLOY_BEGET.md"
else
    echo "❌ Ошибка при сборке проекта!"
    exit 1
fi

