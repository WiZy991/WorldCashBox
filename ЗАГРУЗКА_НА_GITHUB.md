# 🚀 Как загрузить проект на GitHub

## Быстрая инструкция (5 шагов):

### 1️⃣ Откройте терминал в папке проекта

### 2️⃣ Выполните команды по порядку:

```bash
# Проверка Git (должна быть версия)
git --version

# Инициализация (если еще не сделано)
git init

# Настройка (замените на свои данные)
git config user.name "Ваше Имя"
git config user.email "ваш@email.com"

# Добавление всех файлов
git add .

# Создание первого коммита
git commit -m "Initial commit: WorldCashBox website"
```

### 3️⃣ Создайте репозиторий на GitHub:

1. Зайдите на https://github.com
2. Нажмите **"+"** → **"New repository"**
3. Название: `worldcashbox-website`
4. **НЕ** добавляйте README, .gitignore, license
5. Нажмите **"Create repository"**

### 4️⃣ Подключите к GitHub:

**Замените `YOUR_USERNAME` на ваш GitHub username:**

```bash
git remote add origin https://github.com/YOUR_USERNAME/worldcashbox-website.git
git branch -M main
git push -u origin main
```

### 5️⃣ При запросе введите:

- **Username**: ваш GitHub username
- **Password**: Personal Access Token (не пароль!)

---

## 🔑 Как создать Personal Access Token:

1. GitHub → ваш профиль (справа вверху) → **Settings**
2. Внизу слева → **Developer settings**
3. **Personal access tokens** → **Tokens (classic)**
4. **Generate new token (classic)**
5. Название: `WorldCashBox Website`
6. Срок: выберите срок действия
7. Отметьте: ✅ **repo** (полный доступ)
8. Нажмите **Generate token**
9. **Скопируйте токен** (показывается только один раз!)

Используйте этот токен вместо пароля при `git push`.

---

## ✅ Готово!

После успешной загрузки проверьте:
- https://github.com/YOUR_USERNAME/worldcashbox-website

---

## 📝 В будущем для обновления:

```bash
git add .
git commit -m "Описание изменений"
git push
```

---

## ⚠️ Важно:

- ❌ **НЕ загружайте** файл `.env` с секретными ключами
- ✅ **Загружайте** все исходники кода и изображения
- ✅ Папка `node_modules` автоматически игнорируется

